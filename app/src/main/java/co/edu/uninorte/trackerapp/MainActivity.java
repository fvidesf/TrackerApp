package co.edu.uninorte.trackerapp;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {


    @Override
    protected void onCreate(Bundle savedInstanceState) {


        super.onCreate(savedInstanceState);

    }


































































/*
    public void addregistry(View view) {
        Position te=new Position(8.9+i,9.4d+i);
        ruta.add(te);


        i++;
    }

    public void addRoute(View view) {
        routeid = mDatabase.push().getKey();
        Route route=new Route(ruta,"Ruta "+i, new Date());
        mDatabase.child(routeid).setValue(route);
      }

    public void Save(View view) {
        mDatabase.child(routeid).child("route").setValue(ruta);
    }*/
}
